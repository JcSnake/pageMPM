var MAX_PDUS = 38;
var MAX_HRS = 42;

$(document).ready(() => {
    barraPdus();
    barraHrs();
});

function barraPdus() {
    var pdus = $('#barraPdus').data('mpPdus');
    var size = ((70/MAX_PDUS) * pdus) + '%';

    $('#barraPdus')[0].style.width = size;
}

function barraHrs() {
    var hrs = $('#barraHrs').data('mpHrs');
    var size = ((74/MAX_HRS) * hrs) + '%';

    $('#barraHrs')[0].style.width = size;
}